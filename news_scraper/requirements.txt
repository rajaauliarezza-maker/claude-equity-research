requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
feedparser>=6.0.11        # optional but recommended; falls back to lxml XML parser if unavailable

# Google Sheets integration (optional — only needed for `push` command)
gspread>=6.0.0
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
